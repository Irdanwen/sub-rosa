# Docker entrypoint initialization uses only the local Unix socket.
local all postgres trust
local all all scram-sha-256
hostssl subrosa subrosa_runtime all scram-sha-256
hostssl subrosa subrosa_migrator all scram-sha-256
hostssl keycloak keycloak all scram-sha-256
host all all all reject
